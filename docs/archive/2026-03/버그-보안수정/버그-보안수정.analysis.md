# Gap Analysis: 버그-보안수정

> 분석일: 2026-03-28 | Phase: Check (최종)

## 분석 결과 요약

| 항목 | 값 |
|------|-----|
| 분석 대상 | 버그-보안수정 (15개 항목 + resetBtn 스코프 추가 수정) |
| 설계 문서 | `docs/02-design/features/버그-보안수정.design.md` |
| **Match Rate** | **100%** (15/15 구현됨 + 추가 버그 1건 수정) |
| 상태 | 완료 |

---

## Phase별 결과 (최종)

| Phase | 구현 | 전체 | Match Rate |
|-------|:----:|:----:|:----------:|
| Phase 1 (Critical) | 5 | 5 | 100% |
| Phase 2 (High) | 6 | 6 | 100% |
| Phase 3 (Medium) | 4 | 4 | 100% |
| **전체** | **15** | **15** | **100%** |

---

## 항목별 구현 결과

### Phase 1 — Critical (5/5 완료)

| ID | 설명 | 파일 | 결과 |
|----|------|------|------|
| SEC-01 | Edge Function JWT 인증 + CORS | `settle-balance-games/index.ts` | Bearer 토큰 검증 + `https://matbul.com` CORS 적용 |
| BUG-06 | loadMyComment parent_id 필터 | `post.js` | `.is('parent_id', null)` 추가 |
| BUG-05 | 리버튼 크레딧 원자성 | `post.js` | `spend_credits` → 성공 시 `comments.insert` 순서 변경 |
| BUG-03/04 | URL.createObjectURL 메모리 누수 | `create.js` | `communityObjectUrls[]`, `questionObjectUrls[]` 추적 배열 + `revokeObjectURL` |
| BUG-01 | commentList 중복 리스너 | `post.js` | 두 리스너 단일 위임으로 병합 |

### Phase 2 — High (6/6 완료)

| ID | 설명 | 파일 | 결과 |
|----|------|------|------|
| SEC-02 | LIKE 와일드카드 이스케이프 | `home.js` | `%` → `\\%`, `_` → `\\_` 이스케이프 후 ilike |
| SEC-03 | Edge Function CORS 도메인 제한 | `settle-balance-games/index.ts`, `delete-account/index.ts` | `'*'` → `'https://matbul.com'` |
| SEC-04 | escapeHtml 작은따옴표 추가 | `auth.js` | `.replace(/'/g,'&#x27;')` 추가 |
| BUG-07 | fmtNum parseInt 오류 (data-raw) | `profile.js` | `dataset.raw`에 원시값 저장, 팔로우 토글 시 정수 연산 |
| BUG-09 | clearTimeout 누락 | `create.js` | early return 전 `clearTimeout(timeoutId)` 추가 |
| BUG-08 | Promise.all 병렬화 | `profile.js`, `mypage.js` | `await Promise.all([...])` 병렬 처리 |

### Phase 3 — Medium (4/4 완료)

| ID | 설명 | 파일 | 결과 |
|----|------|------|------|
| BUG-10 | 게스트 null 가드 | `post.js` | `if (myComment && currentUser)` 조건 추가 |
| BUG-11 | 타이머 60s 복귀 | `home.js` | `_urgentMode` 플래그 + 중첩 인터벌로 복귀 로직 구현 |
| BUG-18/19 | quiz.js 이모지 → SVG | `quiz.js` | `📭` → mail SVG, `📝` → pencil SVG |
| SEC-10 | UUID 형식 검증 | `profile.js` | `_UUID_RE` 정규식 검증, 실패 시 `index.html` 리다이렉트 |

---

## 추가 수정 (gap-detector 발견)

| ID | 설명 | 파일 | 결과 |
|----|------|------|------|
| BUG-NEW | `resetBtn` 스코프 오류 | `create.js` | `resetBtn`을 `handleSubmit` 공유 스코프로 호이스팅, `if (category === '퀴즈')` 내부 중복 선언 제거 |

---

## 결론

설계 문서의 15개 항목 전체 구현 완료 (Match Rate 100%).
gap-detector 재검증 과정에서 발견된 추가 HIGH 버그(`resetBtn` 스코프)도 수정 완료.

**권장 다음 단계**: `/pdca report 버그-보안수정`
